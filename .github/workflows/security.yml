# yaml-language-server: $schema=https://www.schemastore.org/github-workflow.json
#
# Security workflow for proto repository
# Since this is a protobuf definitions repository with no package dependencies,
# there are no security scans to run. This workflow provides a consistent
# Security Summary job for branch protection rules.

name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs for PRs
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  # Placeholder job - proto repo has no package dependencies to scan
  # This exists to maintain consistent workflow structure across repos
  no-dependencies:
    name: No Dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@df199fb7be9f65074067a9eb93f12bb4c5547cf2 # v2.13.3
        with:
          egress-policy: audit

      - name: Skip security scan
        run: |
          echo "This is a protobuf definitions repository with no package dependencies."
          echo "No security scans required."

  # Security scan summary - provides consistent status check for branch protection
  security-summary:
    name: Security Summary
    needs: [no-dependencies]
    runs-on: ubuntu-latest
    if: always()
    permissions:
      contents: read
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@df199fb7be9f65074067a9eb93f12bb4c5547cf2 # v2.13.3
        with:
          egress-policy: audit

      - name: Check security scan results
        env:
          NO_DEPS_RESULT: ${{ needs.no-dependencies.result }}
        run: |
          echo "## Security Scan Results"
          echo ""
          echo "| Check | Status |"
          echo "|-------|--------|"
          echo "| Dependencies | $NO_DEPS_RESULT (none to scan) |"
          echo ""
          echo "This is a protobuf definitions repository."
          echo "No package dependencies to scan for vulnerabilities."
          echo ""

          # Fail if the placeholder job failed (shouldn't happen)
          if [[ "$NO_DEPS_RESULT" != "success" && "$NO_DEPS_RESULT" != "skipped" ]]; then
            echo "Unexpected failure in security workflow"
            exit 1
          fi

          echo "All security checks passed!"
